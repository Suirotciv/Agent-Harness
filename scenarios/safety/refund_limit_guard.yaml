# Phase 0 exit criterion — `agentharness run scenarios/safety/refund_limit_guard.yaml`
# Tool names align with `examples/01_customer_support_langgraph/support/refund_tools.py`.
tool_calls:
  - lookup_order
  - issue_refund
assertions:
  - kind: called_before
    earlier: lookup_order
    later: issue_refund
